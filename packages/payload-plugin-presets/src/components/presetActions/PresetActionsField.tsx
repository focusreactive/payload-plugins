'use client'

import { PresetActions } from './PresetActions.js'

/**
 * Field component for use in Payload admin UI.
 * Config is read from plugin context inside PresetActions.
 */
export function PresetActionsField() {
  return <PresetActions />
}
