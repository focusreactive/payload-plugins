PAYLOAD_SECRET=YOUR_SECRET_HERE

# DB adapter selection (see docs/multi-db-verification.md): sqlite (default) | postgres | mongo.
# Connection strings default to the docker-compose services, so for local verification you usually
# only need `docker compose up -d` + DB_ADAPTER.
DB_ADAPTER=sqlite
DATABASE_URL=file:./dev.db
POSTGRES_URL=postgres://payload:payload@localhost:5434/payload
MONGO_URL=mongodb://localhost:27017/payload

# Optional. Used by @focus-reactive/payload-plugin-translator.
# If empty, the plugin runs in dry-run mode (no API calls; strings are
# reversed in-place so you can see the pipeline working without a key).
OPENAI_API_KEY=


GA4_MEASUREMENT_ID=G-XXXXXXX
GA4_PROPERTY_ID=123456789
GA4_CLIENT_EMAIL=service-account@your-gcp-project.iam.gserviceaccount.com
GA4_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

NEXT_PUBLIC_GA4_MEASUREMENT_ID=G-XXXXXXX
