{
  "installCommand": "git config --global --add \"url.https://x-access-token:${PRIVATE_REPO_TOKEN}@github.com/.insteadOf\" \"https://www.github.com/\" && bun install",
  "buildCommand": "cd ../.. && bunx turbo run build --filter='./packages/*' && cd apps/cms && bun run migrate && bun run build",
  "crons": [
    {
      "path": "/api/scheduled-publish/run",
      "schedule": "0 * * * *"
    }
  ]
}
