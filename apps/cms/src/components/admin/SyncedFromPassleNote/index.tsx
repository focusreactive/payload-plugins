export const SyncedFromPassleNote = () => {
  return (
    <div className="field-description" style={{ marginBottom: "var(--base)" }}>
      <strong>Synced from Passle.</strong> The title, summary, article and published date are
      written in Passle, so change them there and the next sync brings the change here. The author
      is matched by email address; link one by hand only when nothing matched. The address and
      markets are yours to set.
    </div>
  );
};
